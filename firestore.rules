rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Har user sirf apna data read/write kar sakta hai
    match /users/{userId}/songs/{songId} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId;
    }
  }
}
